# ffmpeg lets the server auto-compress/chunk oversized podcast audio before Whisper
[phases.setup]
aptPkgs = ["ffmpeg"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "node node_modules/.bin/next start -p $PORT"
